spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cbomdb
    username: cbom
    password: cbom123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
server:
  port: 8080
logging:
  level:
    root: INFO
